/**
 *  @file    Type.cpp
 *  @author  Faaux (github.com/Faaux)
 *  @date    10 February 2018
 */

#include "Type.h"

namespace DG
{
// void Test()
//{
//    A* a = new A();
//    A* b = new B();
//    A* c = new C();
//    A* d = new D();
//    A* e = new E();
//
//    // All are themselves
//    Assert(a->IsType<A>());
//    Assert(b->IsType<B>());
//    Assert(c->IsType<C>());
//    Assert(d->IsType<D>());
//    Assert(e->IsType<E>());
//
//    // All are base
//    Assert(a->IsTypeOrDerivedType<TypeBase>());
//    Assert(b->IsTypeOrDerivedType<TypeBase>());
//    Assert(c->IsTypeOrDerivedType<TypeBase>());
//    Assert(d->IsTypeOrDerivedType<TypeBase>());
//    Assert(e->IsTypeOrDerivedType<TypeBase>());
//
//    // Inhertance
//    Assert(b->IsTypeOrDerivedType<A>());
//    Assert(c->IsTypeOrDerivedType<A>());
//    Assert(d->IsTypeOrDerivedType<A>());
//    Assert(e->IsTypeOrDerivedType<A>());
//
//    Assert(d->IsTypeOrDerivedType<B>());
//    Assert(e->IsTypeOrDerivedType<C>());
//
//    delete a;
//    delete b;
//    delete c;
//    delete d;
//    delete e;
//}
}  // namespace DG
